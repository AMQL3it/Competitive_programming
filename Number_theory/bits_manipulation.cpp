/******************************************************************************
 *----------------------------------------------------------------------------*
 *                                                                            *
 *        If the plan does not work, change the plan but not the goal.        *
 *----------------------------------------------------------------------------*
 *        Life is just a life. Lead it, Feel it & Enjoy It Properly.          *
 *        Nothing is impossible. Just believe in yourself & Almighty.         *
 *----------------------------------------------------------------------------*
 *                                                                            *
 *----------------------------------------------------------------------------*
 ******************************************************************************/

/// ########################## Bits Manipulation ##############################

#include<bits/stdc++.h>
using namespace std;

///############################# Typedef Section #############################

typedef unsigned int ui;
typedef long long int ll;
typedef unsigned long long ull;
typedef double db;
typedef long double ldb;

///############################################################################

///############################# Define Section ###############################
#define FST_IO ios_base::sync_with_stdio(false); cin.tie(NULL);cout.tie(NULL);
#define READ          freopen("in.txt","r",stdin)
#define WRITE         freopen("out.txt","w",stdout);
#define nl            "\n"
#define pi            acos(-1.0)
#define mem(arr,val)  memset(arr,val,sizeof(arr))
#define pb            push_back
#define mp            make_pair
#define pr            pair<ll,ll>
#define ff            first
#define ss            second
#define mod           32768
#define INF           1e18
///############################################################################

///############################# Input Section ################################

#define si1(x)        scanf("%d",&x)
#define si2(x,y)      scanf("%d %d", &x, &y)
#define si3(x,y,z)    scanf("%d %d %d", &x, &y, &z)

#define sd1(x)        scanf("%lf",&x)
#define sd2(x,y)      scanf("%lf %lf", &x, &y)
#define sd3(x,y,z)    scanf("%lf %lf %lf", &x, &y, &z)

#define sli1(x)       scanf("%lld",&x)
#define sli2(x,y)     scanf("%lld %lld", &x, &y)
#define sli3(x,y,z)   scanf("%lld %lld %lld", &x, &y, &z)

///############################################################################

///############################# Basic Concepts ###############################

/*
    বিট ম্যানিপুলেশন শেখার জন্য প্রয়োজনীয় টপিকসমূহ
    i. বাইনারি এবং ডেসিমাল রূপান্তর
    বিট ম্যানিপুলেশন বুঝতে হলে প্রথমে বাইনারি সংখ্যা পদ্ধতি সম্পর্কে জ্ঞান থাকতে হবে।

    ii. মাস্কিং (Masking)
    নির্দিষ্ট বিট সেট বা ক্লিয়ার করার জন্য মাস্ক তৈরি করা হয়।
    iii. Set, Clear, Toggle, এবং Check বিট
    Set: কোনো বিটকে 1-এ সেট করতে: n | (1 << i)
    Clear: কোনো বিটকে 0-এ সেট করতে: n & ~(1 << i)
    Toggle: কোনো বিট উল্টাতে: n ^ (1 << i)
    Check: কোনো বিট 1 কিনা তা চেক করতে: (n & (1 << i)) != 0
*/

///############################## Bit Set up ##################################
/*
    1. BitSetUp(int x, int idx)
    এই ফাংশনটি x সংখ্যার idx-তম বিটকে 1 করে দেয়।

    লজিক:
    1 << idx: 1-কে idx-বার বাম দিকে সরিয়ে এমন একটি সংখ্যা তৈরি করা হয় যেখানে কেবল idx-তম বিট 1 এবং বাকি সব 0।
    উদাহরণ: idx = 2 হলে, 00000100।
    x | (1 << idx): বিটওয়াইজ OR অপারেশনের মাধ্যমে idx-তম বিটকে 1 করা হয়।

    উদাহরণ:
        int x = 5; // বাইনারি: 0101
        int idx = 1;
        x = BitSetUp(x, idx); // বাইনারি: 0101 | 0010 = 0111
        cout << x; // আউটপুট: 7
    বর্ণনা:

    আগে: 
        𝑥 = 5 = 0101
    পরে: 
        idx = 1 অবস্থানে বিট 1 করা হয়, ফলে 
        x = 7 = 0111।
*/
int BitSetUp(int x, int idx){ return x = x | (1<<idx); }

///############################################################################

///############################## Bit Unset ###################################
/*
    2. BitUnSet(int x, int idx)
    এই ফাংশনটি x সংখ্যার idx-তম বিটকে 0 করে দেয়।

    লজিক:
        1 << idx: 1-কে idx-বার বাম দিকে সরিয়ে একটি সংখ্যা তৈরি করা হয় যেখানে কেবল idx-তম বিট 1।
        ~(1 << idx): এটির NOT অপারেশনের মাধ্যমে একটি মাস্ক তৈরি হয় যেখানে সব বিট 1 কিন্তু idx-তম বিট 0।
        উদাহরণ: idx = 2 হলে, 11111011।
        x & (~(1 << idx)): বিটওয়াইজ AND অপারেশন ব্যবহার করে idx-তম বিটকে 0 করা হয়।

    উদাহরণ:
        int x = 7; // বাইনারি: 0111
        int idx = 1;
        x = BitUnSet(x, idx); // বাইনারি: 0111 & 1101 = 0101
        cout << x; // আউটপুট: 5

    বর্ণনা:
        আগে: x = 7 = 0111
        পরে: idx = 1 অবস্থানের বিট 0 করা হয়, ফলে 
        x = 5 = 0101।
*/
int BitUnSet(int x, int idx){ return x = x & (~(1<<idx)); }
///############################################################################

///############################## Bit Checking ################################
/*
    3. BitCheking(int x, int idx)
    এই ফাংশনটি চেক করে যে x সংখ্যার idx-তম বিট 1 কিনা।

    লজিক:
        1 << idx: 1-কে idx-বার বাম দিকে সরিয়ে এমন একটি সংখ্যা তৈরি করা হয় যেখানে কেবল idx-তম বিট 1।
        x & (1 << idx): বিটওয়াইজ AND অপারেশনের মাধ্যমে চেক করে যে idx-তম বিট 1 কিনা।
        যদি 1, তবে রেজাল্ট true (অন্যথায় false)।
    উদাহরণ:
        int x = 5; // বাইনারি: 0101
        int idx = 2;
        bool isSet = BitCheking(x, idx); // বাইনারি: 0101 & 0100 = 0100
        cout << isSet; // আউটপুট: 1 (সত্য)

    বর্ণনা:
        x = 5 = 0101, এবং idx = 2 অবস্থানের বিট 1। তাই এটি true।
*/
bool BitCheking(int x, int idx){ return x & (1<<idx); }

///############################################################################

///############################## Bit Toggling ################################

/*
    4. BitToggling(int x, int idx)
    এই ফাংশনটি x সংখ্যার idx-তম বিটকে টগল (অর্থাৎ উল্টে) করে।
    যদি 1 থাকে, তাহলে 0 হবে। আর যদি 0 থাকে, তাহলে 1 হবে।

    লজিক:
        1 << idx: 1-কে idx-বার বাম দিকে সরিয়ে এমন একটি সংখ্যা তৈরি করা হয় যেখানে কেবল idx-তম বিট 1।
        x ^ (1 << idx): বিটওয়াইজ XOR অপারেশনের মাধ্যমে idx-তম বিট টগল করা হয়।
    উদাহরণ:
        int x = 5; // বাইনারি: 0101
        int idx = 1;
        x = BitToggling(x, idx); // বাইনারি: 0101 ^ 0010 = 0111
        cout << x; // আউটপুট: 7

    বর্ণনা:
        আগে: x = 5 = 0101
        পরে: idx = 1 অবস্থানের বিট টগল করে x = 7 = 0111।
*/

bool BitToggling(int x, int idx){ return x = x ^ (1<<idx); }

///############################################################################

///############################## Power Of Two ################################

/*
    5. PowerOfTwo(int x)
    এই ফাংশনটি চেক করে যে কোনো সংখ্যা x কি 2-এর পাওয়ার কিনা।

    লজিক:
        2-এর পাওয়ারের সংখ্যা (যেমন: 1,2,4,8,16) বাইনারিতে কেবল একটি বিট 1 থাকে।
        উদাহরণ: 2=10, 4=100, 8=1000।
        (x & (x - 1)): সংখ্যাটি থেকে এক বিট কমালে ডানপাশের 1 বিট মুছে যায়।
        যদি রেজাল্ট 0 হয়, তবে এটি 2-এর পাওয়ার।
        x && !(x & (x - 1)): এটি নিশ্চিত করে যে x শূন্য নয় এবং ওপরের শর্ত মিলে।

    উদাহরণ ১: 2-এর পাওয়ার 
        int x = 8; // বাইনারি: 1000
        bool isPowerOfTwo = PowerOfTwo(x); // 1000 & 0111 = 0
        cout << isPowerOfTwo; // আউটপুট: 1 (সত্য)

    বর্ণনা:
        x = 8 = 1000, এবং x−1 = 7 = 0111।
        x & (x - 1) = 0, তাই এটি 2-এর পাওয়ার।

    উদাহরণ ২: 2-এর পাওয়ার নয়
        int x = 10; // বাইনারি: 1010
        bool isPowerOfTwo = PowerOfTwo(x); // 1010 & 1001 != 0
        cout << isPowerOfTwo; // আউটপুট: 0 (মিথ্যা)

    বর্ণনা:
        x = 10 = 1010, এবং x−1 = 9 = 1001।
        x & (x - 1) ≠ 0, তাই এটি 2-এর পাওয়ার নয়।
*/
bool PowerOfTwo(int x){ return (x && !(x&(x-1))); }

///############################################################################

///############################## Generate Subsets ############################

/*
    6. generateSubsets(vector<int>& arr)
    এই কোডটি একটি অ্যারের (array) সব সম্ভাব্য সাবসেট (power set) তৈরি করতে বিট ম্যানিপুলেশন ব্যবহার করে।

    মূল ধারণা:
        কোনো অ্যারেতে n সংখ্যক উপাদান থাকলে, তার মোট সম্ভাব্য সাবসেট সংখ্যা 2^n।
        প্রতিটি সাবসেট একটি অনন্য বাইনারি সংখ্যার সাথে সম্পর্কিত (০ থেকে 2^𝑛 − 1 পর্যন্ত)।
        বাইনারি সংখ্যার প্রতিটি বিট নির্ধারণ করে যে অ্যারের নির্দিষ্ট একটি উপাদান সাবসেটে থাকবে কি থাকবে না।
        যদি বিট 1 হয়, তাহলে ওই উপাদান সাবসেটে থাকবে।
        যদি বিট 0 হয়, তাহলে ওই উপাদান সাবসেটে থাকবে না।
    
    কোডের কাজের প্রক্রিয়া:
        বাইরের লুপ (Outer Loop):
            লুপটি 0 থেকে 2^n − 1 পর্যন্ত চলে (যেখানে 1 << n = 2^n)।
            প্রতিটি সংখ্যা (i) একটি সাবসেটের প্রতিনিধিত্ব করে।
        ভেতরের লুপ (Inner Loop):
            i-এর প্রতিটি বিট পরীক্ষা করা হয়:
            যদি i-এর j-তম বিট 1 হয়, তাহলে arr[j] সাবসেটে যোগ করা হয়।
            এটি পরীক্ষা করার জন্য বিটওয়াইজ & এবং 1 << j ব্যবহার করা হয়।
        সাবসেট সংরক্ষণ (Store the Subset):
            একটি সংখ্যা (i) এর সব বিট পরীক্ষা করার পরে, সাবসেট সম্পূর্ণ হয় এবং সেটি subsets ভেক্টরে যোগ করা হয়।

    বাইনারি উপস্থাপনা এবং সাবসেট:

    i(Decimal)	i(Binary)	সাবসেট
        0	        000	    {} (ফাঁকা সেট)
        1	        001	    {1}
        2	        010	    {2}
        3	        011	    {1, 2}
        4	        100	    {3}
        5	        101	    {1, 3}
        6	        110	    {2, 3}
        7	        111	    {1, 2, 3}
*/
vector<vector<int>> generateSubsets(vector<int>& arr) {
    int n = arr.size();
    vector<vector<int>> subsets;

    for (int i = 0; i < (1 << n); i++) {
        vector<int> subset;
        for (int j = 0; j < n; j++) {
            // Check if the j-th bit in i is set (1)
            if (i & (1 << j)) {
                subset.push_back(arr[j]);
            }
        }
        subsets.push_back(subset);
    }

    return subsets;
}

///############################################################################

int main()
{
    // FST_IO
    // call and testing

    return 0;
}

///############################# Practice problems #############################
/*
    i. LeetCode
        Single Number
        Number of 1 Bits
        Power of Two
    ii. HackerRank
        Flipping Bits
        Maximizing XOR
    iii. Codeforces
        Shifting Bits
        XOR Sum
*/